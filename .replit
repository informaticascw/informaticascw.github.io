run = "hugo server --buildDrafts --buildFuture --bind 0.0.0.0 --port 443 --disableLiveReload"
language = "Blank Repl"

hidden = [".git", ".github", ".gitignore", ".gitpod.yml", ".replit", "replit.nix"]

[nix]
channel = "stable-22_11"
